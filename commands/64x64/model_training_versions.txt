Model Training and Testing 64x64
--------------------------------

The models in this file are for an input size of 64x64.

Model training is listed from oldest to newest (newest at the bottom).

[V1] The `_` tag:

    ...commands
