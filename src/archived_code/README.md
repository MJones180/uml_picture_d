This folder contains code which is not being used anymore.

As a warning, the files may:

- Contain no documentation
- Have messy code
- Make no sense
- Not even run

!! **Beware of the code within** !!
